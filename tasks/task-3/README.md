# Task: Implementing the includes method without includes

## Condition

Implement a function that checks whether a string contains a given substring.

## Examples

```
includesString("frontend", "end") → true
includesString("developer", "cat") → false
```
