📝 Challenge: Find the Shortest Word
Objective: Implement a function findShort(s) that takes a string s (a sentence) as an argument and must return the shortest word in that sentence.

Key Conditions:

The input will always be a string; you don't need to handle other data types.

Words in the string will be separated by a single space.

The function must return the word itself, not its length.

Example Usage:

Input String (s) Expected Output (Shortest Word)
"The Smallest word in sentence" "in"
"Just test string" "Just"
