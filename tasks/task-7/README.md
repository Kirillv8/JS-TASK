📝 Challenge: Name to Initials Converter
Objective: Implement a function toInitials(fullName) that takes a person's full name (composed of a first name and a last name, separated by a single space) and converts it into a string of initials, separated by a period.

Key Conditions:

The input string will always contain exactly two words (First Name and Last Name).

The output initials must always be in uppercase, regardless of the case of the input string.

The final format must be I.F. (Initial of First Name, dot, Initial of Last Name, dot).

Example Usage:Input String (fullName)Expected Output (Initials)"Bill Gates""B.G.""elon mask""E.M.""john Smith""J.S."
